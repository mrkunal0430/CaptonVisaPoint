VITE_API_URL=http://localhost:5000/api


#  this is the backend url which is on render - i paste this url in frontend environment variable while hosting it on the vercel - if u want to keep both the url hel t=so u have to keep to .env file one for development and one for production and then u can use the command to start the server in development mode and production mode - for development mode u can use npm run dev and for production mode u can use npm start - this way u can keep both the url in different .env file and use them accordingly

# VITE_API_URL=https://captonvisapoint.onrender.com/api

